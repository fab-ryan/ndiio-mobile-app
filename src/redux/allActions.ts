import { openToast, closeToast } from './slices/toast';
import { getProducts, getProductByCategory } from './slices/product';

export default {
  openToast,
  closeToast,
  getProducts,
  getProductByCategory,
};
